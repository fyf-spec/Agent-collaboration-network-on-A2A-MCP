[project]
name = "agent-a2a"
version = "0.1.0"
description = "Local HTTP A2A coordinator demo with ModelScope LLM client."
requires-python = ">=3.10"
dependencies = [
    "openai>=1.0.0",
    "python-dotenv>=1.0.0",
    "requests>=2.34.2",
    "streamlit>=1.57.0",
]

[tool.uv]
package = false
